[package]
name = "micro_test_account"
version = "1.0.0"
edition = "2018"
authors = []
homepage = ""
repository = "https://github.com/ZKAmoeba-Micro/micro"
license = "Apache-2.0"
keywords = ["blockchain", "micro"]
categories = ["cryptography"]
# This Crate must be published, as it's a dev-dependency of some our library crates.

[dependencies]
micro_types = { path = "../../lib/types", version = "1.0" }
micro_utils = { path = "../../lib/utils", version = "1.0" }
micro_crypto = { path = "../../lib/crypto", version = "1.0" }
micro_basic_types = { path = "../../lib/basic_types", version = "1.0" }

num = { version = "0.3.1", features = ["serde"] }
