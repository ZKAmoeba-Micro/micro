[package]
name = "vm"
version = "0.1.0"
edition = "2018"
authors = []
homepage = ""
repository = "https://github.com/ZKAmoeba-Micro/micro"
license = "Apache-2.0"
keywords = ["blockchain", "micro"]
categories = ["cryptography"]

[dependencies]
zkevm-assembly = { git = "https://github.com/ZKAmoeba-Micro/micro-zkEVM-assembly.git", branch = "main" }
micro_crypto = { path = "../crypto", version = "1.0" }
micro_types = { path = "../types", version = "1.0" }
micro_utils = { path = "../utils", version = "1.0" }
micro_config = { path = "../config", version = "1.0" }
micro_state = {path = "../state", version = "1.0" }
micro_storage = {path = "../storage", version = "1.0" }

zk_evm = {git = "https://github.com/ZKAmoeba-Micro/micro-zk_evm.git", branch = "main"}
micro_contracts = { path = "../contracts" }

hex = "0.4"
thiserror = "1.0"
itertools = "0.10"
once_cell = "1.7"
vlog = { path = "../../lib/vlog", version = "1.0" }
metrics = "0.20"

tracing = "0.1"

[dev-dependencies]
tempfile = "3.0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

